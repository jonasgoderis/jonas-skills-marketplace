schema_version: "1.1"
name: next-version
description: >
  Asking only about the version number. Deciding the level is the skill's job, so it should fire rather than being answered off the cuff.
context:
  scaffold_script: scaffold.sh
