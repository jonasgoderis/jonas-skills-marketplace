schema_version: "1.1"
name: internal-fix-no-docs
tags: [behaviour]
description: >
  A branch whose only change hardens the test script. Nothing documented has
  moved, so the skill should say the docs need no change rather than inventing
  doc work — CLAUDE.md naming the changed script is the near-miss.
context:
  scaffold_script: scaffold.sh
